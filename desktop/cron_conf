# mm  hh DD MM W cmd
# /var/spool/cron/paseu - remember path for this file
   0 */2  *  * * find /tmp/mpv_thumbs_cache -amin +120 -exec rm -r '{}' +
   5 */2  *  * * find /tmp/mpv_thumbs_cache/ -empty -type d -delete
